// ── Support Module Entry Point ───────────────────────────────────────────────
const routes = require("./routes");
const service = require("./service");
const constants = require("./constants");

module.exports = { routes, service, constants };
